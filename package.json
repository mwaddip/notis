{
  "name": "dagsocial",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev": "node packages/node/scripts/dev.mjs"
  }
}
